# Telegram ИТМО Бот Расписания 📅

Бот для Telegram, который показывает актуальное расписание занятий ИТМО с учетом текущей недели (четная/нечетная).

## 🚀 Функциональность

- **📅 Расписание на сегодня** - показывает занятия на текущий день с автоматическим определением типа недели
- **📆 Расписание на дату** - позволяет ввести дату в формате ДД.ММ и получить расписание на выбранный день
- **📅 Расписание на неделю** - показывает полное расписание текущей недели

## 📋 Структура расписания

Расписание организовано в 2 типа недель:
- **Нечетная неделя** (week: 1)
- **Четная неделя** (week: 2)

Каждый тип недели содержит расписание для всех дней от понедельника до воскресенья.

## 🛠 Технологии

- **Python 3.11+**
- **python-telegram-bot==21.7** - библиотека для работы с Telegram Bot API
- **Flask==3.0.0** - веб-фреймворк для создания веб-сервера и health check
- **Render** - платформа для деплоя веб-приложений

## 📦 Установка и запуск локально

1. **Клонируйте репозиторий:**
   ```bash
   git clone <your-repo-url>
   cd telegram-itmo-bot-
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate     # Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**


   Создайте файл `.env` на основе `env_example.txt`:
   ```bash
   cp env_example.txt .env
   ```

   Заполните `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   SCHEDULE_JSON={"schedule":[{"week":1,"days":[...]}]}
   ```

   **Важно:** Полное расписание находится в файле `env_example.txt` в переменной `SCHEDULE_JSON`

5. **Запустите бота:**
   ```bash
   python main.py
   ```

## 🚀 Деплой на Render

### Шаг 1: Подготовка репозитория

1. Убедитесь, что код загружен на GitHub
2. Убедитесь, что в репозитории есть файлы:
   - `main.py` - основной бот
   - `web_server.py` - веб-сервер с health check
   - `requirements.txt` - зависимости (включая Flask)
   - `runtime.txt` (содержит `python-3.11.10`)

### Шаг 1.5: Подготовка к повторному деплою

Если нужно обновить существующий сервис:

```bash
python prepare_for_render.py
git add .
git commit -m "Fix Python version compatibility for Render"
git push
```

Render автоматически запустит повторный деплой.

### Шаг 2: Создание веб-сервиса на Render

1. Зайдите на [render.com](https://render.com) и войдите в аккаунт
2. Нажмите "New +" и выберите "Web Service"
3. Выберите ваш GitHub репозиторий
4. Настройте сервис:

   **Name:** `telegram-itmo-bot`

   **Runtime:** `Python 3`

   **Build Command:**
   ```bash
   pip install -r requirements.txt
   ```

   **Start Command:**
   ```bash
   python main.py
   ```

   **Или с явным указанием порта:**
   ```bash
   PORT=10000 python main.py
   ```

### Шаг 3: Настройка переменных окружения

В разделе **Environment** добавьте переменные:

1. **TELEGRAM_BOT_TOKEN:**
   ```
   your_bot_token_here
   ```

2. **SCHEDULE_JSON:** Скопируйте значение переменной `SCHEDULE_JSON` из файла `env_example.txt`

   **Важно:** Полное расписание находится в файле `env_example.txt` в переменной `SCHEDULE_JSON`. Скопируйте всю строку целиком в одну строку.

### Шаг 4: Деплой

1. **Если сервис уже создан:** Render автоматически запустит повторный деплой после push в GitHub
2. **Если создаете новый сервис:** Нажмите "Create Web Service"
3. Дождитесь завершения деплоя (обычно 2-3 минуты)
4. Бот будет запущен автоматически и доступен по URL типа `https://your-service-name.onrender.com`

## 🚀 Как работает бот (режим Polling)

Бот работает в режиме **polling** - он сам периодически опрашивает Telegram API:

### ✅ **Преимущества polling:**

- **Простая настройка** - не нужны вебхуки или дополнительные настройки
- **Надежная работа** - бот автоматически переподключается при потере связи
- **Работает сразу** - достаточно токена и расписания в переменных окружения
- **Меньше настроек** - идеально для простых ботов

### 🔄 **Как это работает:**

1. Бот подключается к Telegram API с вашим токеном
2. **Автоматически удаляет вебхук** (если он был настроен)
3. Каждые несколько секунд проверяет новые сообщения
4. Обрабатывает команды `/start`, кнопки, текстовые сообщения
5. Отвечает пользователям актуальным расписанием

### 📋 **Что нужно:**

- ✅ Токен бота в `TELEGRAM_BOT_TOKEN`
- ✅ Расписание в `SCHEDULE_JSON`
- ✅ Сервер на Render

**Бот готов к работе сразу после деплоя и автоматически очистит старые вебхуки!**

## 🏗️ Архитектура решения (Веб-сервер + Health Check)

Для обеспечения **бесконечной работы** на Render используется комбинированная архитектура:

### ✅ **Почему это работает:**

1. **Веб-сервер** отвечает на запросы Render и предотвращает "засыпание" сервиса
2. **Health Check** (`/health`) сообщает Render, что сервис живой
3. **Бот работает параллельно** в отдельном потоке с long polling
4. **Мониторинг статуса** доступен через веб-интерфейс

### 🌐 **Веб-интерфейс:**

После деплоя доступны следующие эндпоинты:

- **`/`** - Главная страница с информацией о статусе бота
- **`/health`** - Health check для Render (возвращает `{"status": "healthy"}`)
- **`/status`** - JSON статус с детальной информацией о боте

### 🔧 **Как это устроено:**

```
┌─────────────────┐    ┌──────────────────┐
│   Render        │    │   Ваш сервис     │
│   (Port Check)  │◄──►│   (Порт 10000)   │
└─────────────────┘    └──────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   Главный поток    │
                    │                     │
                    │  - Telegram Bot    │
                    │    (Long Polling)  │
                    │  - asyncio loop    │
                    └─────────┬───────────┘
                              │
                    ┌─────────▼──────────┐
                    │   Веб-сервер        │
                    │   (Flask/Werkzeug)  │
                    │   (Отдельный поток) │
                    │                     │
                    │  - Health Check     │
                    │  - Статус бота      │
                    │  - Мониторинг       │
                    └────────────────────┘
```

### 📊 **Преимущества решения:**

- ✅ **Бесконечная работа** - Render не завершает сервис
- ✅ **Мониторинг** - можно проверить статус бота через браузер
- ✅ **Надежность** - бот перезапускается при ошибках
- ✅ **Прозрачность** - видно когда бот активен

### 🚨 **Важно для Render:**

- **Автоматический порт** - Render устанавливает переменную `PORT` (обычно 10000)
- **Можно указать порт вручную** в настройках сервиса Render
- **Health Check** - Render периодически проверяет `/health`
- **Без вебхуков** - бот использует long polling, веб-сервер только для мониторинга

### 🔧 **Настройка порта в Render:**

**Вариант 1: Автоматический порт (рекомендую)**
- Оставьте настройки по умолчанию
- Render сам выберет порт (обычно 10000)
- Код автоматически подхватит порт из переменной `PORT`

**Вариант 2: Ручное указание порта**
1. В Render Dashboard → Settings → Environment
2. Добавьте переменную: `PORT = 10000`
3. Или укажите порт в команде запуска

**Вариант 3: Порт в команде запуска**
В Build Settings измените Start Command:
```bash
PORT=10000 python main.py
```

## 📝 Использование бота

### 🚀 **Быстрый старт:**

1. **Запустите бота** командой `/start` или нажмите кнопку в меню
2. **Выберите действие:**
   - **📅 Сегодня** - расписание на текущий день
   - **📆 Конкретная дата** - введите дату в формате ДД.ММ
   - **📅 На неделю** - полное расписание текущей недели

### ✨ **Удобство использования:**

- **Меню всегда доступно** - после каждого действия бот показывает меню снова
- **Не нужно заново писать /start** - достаточно нажать кнопку
- **Интуитивный интерфейс** - все команды в одном месте

### 🎨 **Формат расписания**

Бот показывает расписание в **минималистичном и читаемом формате**:

```
📚 Название предмета
⏰ 09:00 - 10:30 • Ауд. 101
📍 Адрес корпуса

📚 Название предмета
⏰ 10:40 - 12:10 • Ауд. 101
📍 Адрес корпуса
```

### 🔄 **Автоматическое определение недели**

Бот автоматически определяет тип недели (четная/нечетная) на основе текущей даты:

- Базовая дата: 19 октября 2025 (воскресенье) - конец первой (нечетной) недели
- При каждом запросе расписания бот рассчитывает количество недель с базовой даты
- Если прошло четное количество недель с базовой даты - используется нечетная неделя
- Если прошло нечетное количество недель с базовой даты - используется четная неделя
- Пользователь не видит техническую информацию о типе недели - только актуальное расписание

## 🚨 Важные замечания

- Расписание хранится в переменной окружения `SCHEDULE_JSON`, а не в репозитории
- Бот **автоматически определяет тип недели** (четная/нечетная) при каждом запросе расписания
- При нажатии кнопки "📅 Сегодня" бот сам понимает текущую дату и показывает актуальное расписание
- Поддерживается формат даты ДД.ММ (например: 25.12 для 25 декабря)
- Воскресенье отмечается как выходной день

## 🛠 Разработка

Для локальной разработки используйте токен из переменной окружения или добавьте его в `.env` файл.